pluginManagement {
    repositories {
        gradlePluginPortal()
    }

    includeBuild('gradle/plugins')
}

dependencyResolutionManagement {
    repositories {
        mavenCentral()
    }
}

rootProject.name = "ProyectoJava"

include('contexto')
include('capa-negocio')
include('capa-datos'

)